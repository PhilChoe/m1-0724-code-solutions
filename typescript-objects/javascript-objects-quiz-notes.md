# typescript-objects-quiz-notes

## Quiz Questions

Answer the following questions in the provided markdown file before turning in this exercise:

- What are objects used for?
  //organizing data, reusability, encapsulation, prototypal inheritance, built-in objects
- What are object properties?
  //data about the object, encapsulation within the curly braces
- Describe object literal notation.
  //declaring a variable = and putting in object properties within the curly braces
- How do you remove a property from an object?
  //using the delete operator and either the dot notation or the bracket notation of what you want to remove
- What are the two ways to get or update the value of a property?
  //the dot notation or the bracket notation
- What is the purpose of interfaces in TypeScript?
  //it defines the shape of an object, you can use the interface to annotate any objects in the code that match the structure
- What are two different ways to describe the shape of an object in TypeScript?
  //embedded within the curly braces, you set the variable and set the typeof you wish for it to take
- How do optional properties in a TypeScript interface differ from mandatory properties, and why are they useful?
  //they are followed by a ? and does not have to be included in the properties for code to execute

## Notes

All student notes should be written here.

How to write `Code Examples` in markdown

for JS:

```javascript
const data = 'Howdy';
```

for HTML:

```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:

```css
div {
  width: 100%;
}
```
